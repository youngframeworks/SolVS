# Agent: SolManager (VS Code copy)

Cloud-compatible orchestrator profile for local VS Code execution.

- Human-gated self-heal and self-evolve
- Drift scoring and architecture review
- Deterministic reporting to `runtime/reports/`
